# M1.5 — Derivazione percentile soglie vol regime Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Derivare offline, da dati storici reali BTC/ETH, le soglie assolute enter/exit del regime layer v0 (che oggi sono valori scelti a mano, non ancorati), e scriverle nel config con provenienza documentata — senza toccare la macchina a stati già approvata (Task 4-9 del piano M1).

**Architecture:** Un modulo puro e testabile (`src/regime/threshold_derivation.py`) che simula il comportamento della macchina a stati esistente (riusa `regime.hysteresis.next_state`, NON lo reimplementa) su una serie storica di vol e ne misura frazione di tempo in high-vol, transizioni/anno, dwell time. Uno script CLI separato (`scripts/derive_vol_thresholds.py`) che fa I/O reale (fetch OHLCV da OKX/Kraken via ccxt) e usa il modulo puro per proporre soglie candidate. Solo il modulo puro ha unit test (dati sintetici); lo script va eseguito per davvero contro dati reali — il suo output IN SÉ è il deliverable da mostrare in review, non un test automatizzato.

**Tech Stack:** Python >=3.11, pandas (già dipendenza), ccxt (già dipendenza, OKX + Kraken), pytest, ruff.

## Global Constraints

- **Architettura (pre-registrata, non riaprire):** soglie derivate OFFLINE, scritte come valori assoluti statici nel config, con provenienza documentata. Nessun calcolo percentile a runtime nel layer v0 (annotare l'opzione percentile-a-runtime come possibile evoluzione meta-layer, non implementarla). La macchina a stati (`regime/vol_state.py`, `regime/hysteresis.py`, `regime/store.py`) NON si tocca in nessun task di questo piano.
- **Stimatore:** EWMA `span=32` (ancoraggio RiskMetrics λ=0.94, vedi `docs/nota-ancoraggio-soglie-vol-regime-2026-07.md`), riusando `regime.vol_state.compute_ewma_vol` esistente — non reimplementarlo.
- **Dati:** daily candle BTC/USDT ed ETH/USDT da OKX via ccxt, storia dal 2019-01-01. Se OKX non copre fino a quella data, fallback Kraken per il segmento mancante, con test di sanità (correlazione dei rendimenti nel periodo di overlap) riportato nell'output dello script.
- **Criterio soglie (pre-registrato, non riaprire):** frazione di tempo target in high-vol 20-25% sulla storia completa; tra i candidati che rispettano il target, tenere solo quelli con mediana transizioni/anno <= 8 E dwell time minimo osservato >= 3 giorni. Riportare, per la soglia scelta: frazione effettiva, transizioni/anno per anno solare, distribuzione dwell time — **per BTC ed ETH separatamente, nessun segnale combinato** (chiarito esplicitamente con Andrea: il layer ha due stati indipendenti, una eventuale combinazione è una decisione di design di M2).
- **Niente backtest/PnL:** si misura solo il comportamento della macchina a stati sulla storia, mai un risultato economico.
- **TDD dove il codice è puro e testabile** (modulo di derivazione); lo script CLI di fetch dati è validato eseguendolo per davvero, non con unit test di rete.
- Conventional commits (`feat:`, `test:`, `docs:`, `chore:`). Ogni task chiude con suite completa verde (`python -m pytest -v`) + `python -m ruff check src tests` pulito — stesso vincolo del piano M1.
- **REVIEW BLOCCANTE dopo il Task 2**, prima di toccare `config/regime.yaml`: mostrare ad Andrea la tabella di metriche per le soglie proposte e attendere conferma esplicita prima del Task 3.

---

## File Structure

```
D:\Claude\orchestrator\
  src\regime\
    threshold_derivation.py     # Task 1 — puro, testabile
  scripts\
    derive_vol_thresholds.py    # Task 2 — I/O reale (ccxt OKX/Kraken), eseguito per davvero
  tests\regime\
    test_threshold_derivation.py  # Task 1
  config\
    regime.yaml                 # Task 3 — MODIFICATO dopo review bloccante
  docs\
    regime-threshold-provenance-2026-07.md  # Task 3 — NUOVO, provenienza + prossima ri-derivazione
```

---

### Task 1: Modulo puro di derivazione soglie

**Files:**
- Create: `src/regime/threshold_derivation.py`
- Test: `tests/regime/test_threshold_derivation.py`

**Interfaces:**
- Consumes: `regime.hysteresis.HysteresisBand`, `regime.hysteresis.next_state` (Task 4 del piano M1, già approvato — NON reimplementare la logica di isteresi qui, solo simularne l'applicazione su una serie storica).
- Produces: `count_transitions(states: pd.Series) -> int`, `dwell_times(states: pd.Series) -> list[int]`, `fraction_high_vol(states: pd.Series) -> float`, `transitions_per_calendar_year(states: pd.Series) -> dict[int, int]` (richiede `states.index` sia un `DatetimeIndex`), `simulate_hysteresis_path(vol_series: pd.Series, band: HysteresisBand, initial_state: bool = False) -> pd.Series`, `ThresholdCandidateMetrics` (dataclass frozen: `enter, exit, fraction_high_vol, transitions_per_year, median_transitions_per_year, dwell_times, min_dwell_time`), `evaluate_threshold_candidate(vol_series: pd.Series, enter: float, exit: float) -> ThresholdCandidateMetrics`, `ThresholdSearchCriterion` (dataclass frozen: `target_fraction_low, target_fraction_high, max_median_transitions_per_year, min_dwell_time_days`), `search_threshold_candidates(vol_series: pd.Series, enter_percentiles: list[float], exit_percentiles: list[float], criterion: ThresholdSearchCriterion) -> list[ThresholdCandidateMetrics]`. Consumato da Task 2 (`scripts/derive_vol_thresholds.py`).

- [ ] **Step 1: Write the failing tests**

```python
# tests/regime/test_threshold_derivation.py
from __future__ import annotations

import pandas as pd
import pytest

from regime.hysteresis import HysteresisBand
from regime.threshold_derivation import (
    ThresholdCandidateMetrics,
    ThresholdSearchCriterion,
    count_transitions,
    dwell_times,
    evaluate_threshold_candidate,
    fraction_high_vol,
    search_threshold_candidates,
    simulate_hysteresis_path,
    transitions_per_calendar_year,
)


def test_count_transitions_counts_state_changes():
    states = pd.Series([False, False, True, True, False, True])
    assert count_transitions(states) == 3


def test_count_transitions_constant_series_has_zero_transitions():
    assert count_transitions(pd.Series([True, True, True])) == 0


def test_count_transitions_single_element_has_zero_transitions():
    assert count_transitions(pd.Series([True])) == 0


def test_dwell_times_returns_run_lengths():
    states = pd.Series([False, False, True, True, True, False, True])
    assert dwell_times(states) == [2, 3, 1, 1]


def test_dwell_times_single_run():
    assert dwell_times(pd.Series([True, True, True])) == [3]


def test_fraction_high_vol_computes_mean():
    states = pd.Series([True, True, False, False])
    assert fraction_high_vol(states) == pytest.approx(0.5)


def test_transitions_per_calendar_year_includes_years_with_zero_transitions():
    idx = pd.DatetimeIndex(
        ["2023-12-30", "2023-12-31", "2024-01-01", "2024-01-02", "2024-06-01"]
    )
    states = pd.Series([False, False, True, True, False], index=idx)
    result = transitions_per_calendar_year(states)
    assert result == {2023: 0, 2024: 2}


def test_simulate_hysteresis_path_reproduces_next_state_semantics():
    band = HysteresisBand(enter=0.8, exit=0.6)
    vol_series = pd.Series([0.9, 0.7, 0.5, 0.85])
    result = simulate_hysteresis_path(vol_series, band)
    expected = pd.Series([True, True, False, True])
    pd.testing.assert_series_equal(result, expected, check_names=False)


def test_simulate_hysteresis_path_accepts_custom_initial_state():
    band = HysteresisBand(enter=0.8, exit=0.6)
    vol_series = pd.Series([0.7])
    result = simulate_hysteresis_path(vol_series, band, initial_state=True)
    assert result.iloc[0] is True or result.iloc[0] == True  # noqa: E712


def test_evaluate_threshold_candidate_computes_all_metrics():
    idx = pd.date_range("2024-01-01", periods=4, freq="D")
    vol_series = pd.Series([0.9, 0.7, 0.5, 0.85], index=idx)
    metrics = evaluate_threshold_candidate(vol_series, enter=0.8, exit=0.6)
    assert isinstance(metrics, ThresholdCandidateMetrics)
    assert metrics.enter == 0.8
    assert metrics.exit == 0.6
    assert metrics.fraction_high_vol == pytest.approx(0.75)  # 3 True su 4
    assert metrics.dwell_times == [2, 1, 1]  # True,True | False | True
    assert metrics.min_dwell_time == 1


def test_search_threshold_candidates_filters_by_criterion():
    idx = pd.date_range("2024-01-01", periods=100, freq="D")
    vol_series = pd.Series([1.0] * 25 + [0.3] * 75, index=idx)
    criterion = ThresholdSearchCriterion(
        target_fraction_low=0.20,
        target_fraction_high=0.30,
        max_median_transitions_per_year=8.0,
        min_dwell_time_days=3,
    )
    candidates = search_threshold_candidates(
        vol_series,
        enter_percentiles=[0.70, 0.80],
        exit_percentiles=[0.30, 0.50],
        criterion=criterion,
    )
    assert len(candidates) > 0
    best = candidates[0]
    assert criterion.target_fraction_low <= best.fraction_high_vol <= criterion.target_fraction_high


def test_search_threshold_candidates_returns_empty_when_nothing_satisfies_criterion():
    idx = pd.date_range("2024-01-01", periods=10, freq="D")
    vol_series = pd.Series([0.5] * 10, index=idx)  # costante: mai high-vol con nessuna soglia sensata
    criterion = ThresholdSearchCriterion(
        target_fraction_low=0.90,
        target_fraction_high=1.0,
        max_median_transitions_per_year=8.0,
        min_dwell_time_days=3,
    )
    candidates = search_threshold_candidates(
        vol_series,
        enter_percentiles=[0.80],
        exit_percentiles=[0.20],
        criterion=criterion,
    )
    assert candidates == []
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd D:/Claude/orchestrator && python -m pytest tests/regime/test_threshold_derivation.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'regime.threshold_derivation'`

- [ ] **Step 3: Write minimal implementation**

```python
# src/regime/threshold_derivation.py
"""Derivazione offline delle soglie assolute enter/exit per il regime layer
v0 (ADR-036 §3, milestone M1.5). Modulo PURO: nessun I/O, nessuna chiamata
di rete. Simula la macchina a stati esistente (`regime.hysteresis`, MAI
reimplementata qui) su una serie storica di vol per misurare frazione di
tempo in high-vol, transizioni/anno e dwell time — le metriche del
criterio pre-registrato (target 20-25% high-vol, mediana transizioni/anno
<= 8, dwell time minimo >= 3 giorni). Le soglie risultanti vengono scritte
come valori assoluti statici nel config (Task 3): nessun calcolo
percentile a runtime nel layer v0."""

from __future__ import annotations

import statistics
from dataclasses import dataclass

import pandas as pd

from regime.hysteresis import HysteresisBand, next_state


def count_transitions(states: pd.Series) -> int:
    """Numero di cambi di stato lungo la serie (confronto con l'osservazione precedente)."""
    if len(states) < 2:
        return 0
    return int((states.values[1:] != states.values[:-1]).sum())


def dwell_times(states: pd.Series) -> list[int]:
    """Lunghezza (in osservazioni) di ogni run consecutivo dello stesso stato."""
    if len(states) == 0:
        return []
    runs: list[int] = []
    current_value = states.iloc[0]
    current_length = 1
    for value in states.iloc[1:]:
        if value == current_value:
            current_length += 1
        else:
            runs.append(current_length)
            current_value = value
            current_length = 1
    runs.append(current_length)
    return runs


def fraction_high_vol(states: pd.Series) -> float:
    """Frazione di osservazioni in stato True (high-vol)."""
    return float(states.mean())


def transitions_per_calendar_year(states: pd.Series) -> dict[int, int]:
    """Transizioni per anno solare, attribuite all'anno in cui la NUOVA
    osservazione (quella diversa dalla precedente) cade. Include tutti gli
    anni presenti nell'indice, anche con zero transizioni. Richiede
    `states.index` sia un DatetimeIndex."""
    years = states.index.year
    result = dict.fromkeys(sorted(set(years)), 0)
    changed = states.values[1:] != states.values[:-1]
    for is_change, year in zip(changed, years[1:], strict=True):
        if is_change:
            result[year] += 1
    return result


def simulate_hysteresis_path(
    vol_series: pd.Series, band: HysteresisBand, initial_state: bool = False
) -> pd.Series:
    """Applica `regime.hysteresis.next_state` in sequenza lungo `vol_series`,
    restituendo la serie di stati risultante (stesso indice di `vol_series`)."""
    state = initial_state
    output = []
    for value in vol_series:
        state = next_state(state, value, band)
        output.append(state)
    return pd.Series(output, index=vol_series.index)


@dataclass(frozen=True)
class ThresholdCandidateMetrics:
    enter: float
    exit: float
    fraction_high_vol: float
    transitions_per_year: dict[int, int]
    median_transitions_per_year: float
    dwell_times: list[int]
    min_dwell_time: int


def evaluate_threshold_candidate(
    vol_series: pd.Series, enter: float, exit: float
) -> ThresholdCandidateMetrics:
    """Valuta una coppia (enter, exit) candidata simulando la macchina a
    stati sulla storia reale e misurando tutte le metriche del criterio
    pre-registrato."""
    band = HysteresisBand(enter=enter, exit=exit)
    states = simulate_hysteresis_path(vol_series, band)
    tpy = transitions_per_calendar_year(states)
    dwell = dwell_times(states)
    return ThresholdCandidateMetrics(
        enter=enter,
        exit=exit,
        fraction_high_vol=fraction_high_vol(states),
        transitions_per_year=tpy,
        median_transitions_per_year=statistics.median(tpy.values()) if tpy else 0.0,
        dwell_times=dwell,
        min_dwell_time=min(dwell) if dwell else 0,
    )


@dataclass(frozen=True)
class ThresholdSearchCriterion:
    target_fraction_low: float
    target_fraction_high: float
    max_median_transitions_per_year: float
    min_dwell_time_days: int


def search_threshold_candidates(
    vol_series: pd.Series,
    enter_percentiles: list[float],
    exit_percentiles: list[float],
    criterion: ThresholdSearchCriterion,
) -> list[ThresholdCandidateMetrics]:
    """Genera candidati (enter, exit) dai percentili storici di
    `vol_series` (soglie DERIVATE offline da qui, mai calcolate a runtime
    nel layer v0), valuta ciascuno, e restituisce solo quelli che
    rispettano il criterio pre-registrato — ordinati per vicinanza al
    centro del range di frazione target (il primo elemento è il candidato
    migliore)."""
    target_center = (criterion.target_fraction_low + criterion.target_fraction_high) / 2
    candidates: list[ThresholdCandidateMetrics] = []

    for enter_pct in enter_percentiles:
        enter_value = float(vol_series.quantile(enter_pct))
        for exit_pct in exit_percentiles:
            exit_value = float(vol_series.quantile(exit_pct))
            if exit_value >= enter_value:
                continue

            metrics = evaluate_threshold_candidate(vol_series, enter_value, exit_value)

            if not (
                criterion.target_fraction_low
                <= metrics.fraction_high_vol
                <= criterion.target_fraction_high
            ):
                continue
            if metrics.median_transitions_per_year > criterion.max_median_transitions_per_year:
                continue
            if metrics.min_dwell_time < criterion.min_dwell_time_days:
                continue

            candidates.append(metrics)

    return sorted(candidates, key=lambda m: abs(m.fraction_high_vol - target_center))
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd D:/Claude/orchestrator && python -m pytest tests/regime/test_threshold_derivation.py -v`
Expected: PASS (11 test)

- [ ] **Step 5: Run the full suite and ruff**

Run: `cd D:/Claude/orchestrator && python -m pytest -v` — expected: tutti i test passano (62 precedenti + 11 nuovi = 73).
Run: `cd D:/Claude/orchestrator && python -m ruff check src tests` — expected: `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add src/regime/threshold_derivation.py tests/regime/test_threshold_derivation.py
git commit -m "feat: modulo puro di derivazione soglie vol regime (M1.5)"
```

---

### Task 2: Script di derivazione (dati reali OKX/Kraken)

**Files:**
- Create: `scripts/derive_vol_thresholds.py`

**Interfaces:**
- Consumes: `regime.vol_state.compute_ewma_vol` (riusato, NON reimplementato), `regime.threshold_derivation.ThresholdSearchCriterion`, `regime.threshold_derivation.search_threshold_candidates` (Task 1).
- Produces: nessuna interfaccia consumata da altri task — l'output di questo task è il REPORT testuale stampato dallo script quando eseguito, che alimenta la review bloccante e poi il Task 3.

**Nota importante sul metodo:** questo task NON segue il ciclo TDD standard (non c'è una logica pura da testare con dati sintetici — il punto dello script è fare I/O reale contro OKX/Kraken). Il "test" di questo task è **eseguire davvero lo script e riportare l'output reale** nel report del task. Non mockare le chiamate ccxt: il deliverable sono i numeri veri.

- [ ] **Step 1: Scrivere lo script**

```python
# scripts/derive_vol_thresholds.py
"""Script di derivazione delle soglie vol regime da dati storici reali
(M1.5). Esegue fetch OHLCV giornaliero BTC/USDT ed ETH/USDT da OKX (con
fallback Kraken se OKX non copre fino al 2019-01-01, con test di sanità
sul periodo di overlap), calcola vol EWMA (span=32, stesso stimatore di
`regime.vol_state.compute_ewma_vol`), e propone soglie enter/exit per
ciascun asset separatamente secondo il criterio pre-registrato (frazione
target 20-25% high-vol, mediana transizioni/anno <= 8, dwell time minimo
>= 3 giorni).

Uso: python scripts/derive_vol_thresholds.py

NON esegue ordini, non richiede chiavi (solo endpoint pubblici OHLCV).
NON scrive config/regime.yaml da solo: l'output va rivisto da Andrea
prima di qualunque modifica al config (Task 3, dopo review bloccante)."""

from __future__ import annotations

import statistics
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

import ccxt
import pandas as pd

from regime.threshold_derivation import ThresholdSearchCriterion, search_threshold_candidates
from regime.vol_state import compute_ewma_vol

EWMA_SPAN = 32
SINCE_DATE = "2019-01-01"
CRITERION = ThresholdSearchCriterion(
    target_fraction_low=0.20,
    target_fraction_high=0.25,
    max_median_transitions_per_year=8.0,
    min_dwell_time_days=3,
)
ENTER_PERCENTILES = [0.60, 0.65, 0.70, 0.75, 0.80, 0.85, 0.90]
EXIT_PERCENTILES = [0.40, 0.45, 0.50, 0.55, 0.60, 0.65, 0.70]


def fetch_daily_ohlcv(exchange, symbol: str, since_ms: int) -> pd.DataFrame:
    """Fetch paginato di candele giornaliere da `exchange` per `symbol`
    a partire da `since_ms` (epoch millisecondi) fino a oggi."""
    all_candles: list[list] = []
    limit = 300
    while True:
        candles = exchange.fetch_ohlcv(symbol, timeframe="1d", since=since_ms, limit=limit)
        if not candles:
            break
        all_candles.extend(candles)
        last_ts = candles[-1][0]
        next_since = last_ts + 24 * 60 * 60 * 1000
        if next_since <= since_ms:
            break
        since_ms = next_since
        if len(candles) < limit:
            break

    df = pd.DataFrame(all_candles, columns=["timestamp", "open", "high", "low", "close", "volume"])
    df["date"] = pd.to_datetime(df["timestamp"], unit="ms", utc=True)
    return df.drop_duplicates(subset="date").set_index("date").sort_index()


def fetch_with_fallback(asset: str) -> tuple[pd.DataFrame, str]:
    """Fetch OKX come fonte primaria; fallback Kraken per il segmento
    mancante se OKX non copre fino a SINCE_DATE, con test di sanità
    (correlazione dei rendimenti nel periodo di overlap) stampato."""
    since_ms = int(pd.Timestamp(SINCE_DATE, tz="UTC").timestamp() * 1000)
    target_start = pd.Timestamp(SINCE_DATE, tz="UTC")

    okx = ccxt.okx()
    okx_df = fetch_daily_ohlcv(okx, f"{asset}/USDT", since_ms)
    earliest_okx = okx_df.index.min()

    if earliest_okx <= target_start + pd.Timedelta(days=7):
        return okx_df, "OKX (copertura completa dal {})".format(SINCE_DATE)

    kraken = ccxt.kraken()
    kraken_df = fetch_daily_ohlcv(kraken, f"{asset}/USD", since_ms)

    overlap_start = max(kraken_df.index.min(), earliest_okx)
    overlap_end = min(kraken_df.index.max(), okx_df.index.max())
    if overlap_start < overlap_end:
        okx_ret = okx_df.loc[overlap_start:overlap_end, "close"].pct_change().dropna()
        kraken_ret = kraken_df.loc[overlap_start:overlap_end, "close"].pct_change().dropna()
        aligned = pd.concat([okx_ret.rename("okx"), kraken_ret.rename("kraken")], axis=1).dropna()
        correlation = aligned["okx"].corr(aligned["kraken"])
        print(
            f"[sanity check {asset}] correlazione rendimenti OKX vs Kraken "
            f"nel periodo di overlap ({overlap_start.date()} - {overlap_end.date()}, "
            f"n={len(aligned)}): {correlation:.4f}"
        )

    combined = pd.concat([kraken_df.loc[: earliest_okx - pd.Timedelta(days=1)], okx_df])
    combined = combined[~combined.index.duplicated(keep="last")].sort_index()
    source = f"Kraken ({SINCE_DATE} - {earliest_okx.date()}) + OKX (da {earliest_okx.date()})"
    return combined, source


def main() -> None:
    for asset in ("BTC", "ETH"):
        df, source = fetch_with_fallback(asset)
        returns = df["close"].pct_change().dropna()
        vol = compute_ewma_vol(returns, span=EWMA_SPAN).dropna()

        candidates = search_threshold_candidates(vol, ENTER_PERCENTILES, EXIT_PERCENTILES, CRITERION)

        print(f"\n=== {asset} — fonte dati: {source} ===")
        print(f"Periodo vol: {vol.index.min().date()} - {vol.index.max().date()} ({len(vol)} osservazioni)")

        if not candidates:
            print(
                "NESSUN CANDIDATO rispetta il criterio pre-registrato con questa griglia "
                "di percentili. Riportare ad Andrea prima di allargare la griglia o "
                "rivedere il criterio."
            )
            continue

        best = candidates[0]
        print(f"Soglia proposta: enter={best.enter:.4f}, exit={best.exit:.4f}")
        print(f"Frazione tempo high-vol: {best.fraction_high_vol:.2%} (target 20-25%)")
        print(f"Transizioni/anno per anno solare: {best.transitions_per_year}")
        print(f"Mediana transizioni/anno: {best.median_transitions_per_year}")
        print(f"Dwell time minimo osservato: {best.min_dwell_time} giorni (soglia minima 3)")
        print(
            f"Distribuzione dwell time: min={min(best.dwell_times)}, "
            f"mediana={statistics.median(best.dwell_times)}, max={max(best.dwell_times)}"
        )
        print(f"Candidati totali che rispettano il criterio: {len(candidates)}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Eseguire lo script per davvero**

Run: `cd D:/Claude/orchestrator && python scripts/derive_vol_thresholds.py`

Nessun "expected output" pre-scritto qui: i numeri sono il deliverable reale di questo task,
dipendono dalla vera storia di mercato. Copiare l'output completo (entrambi gli asset) nel
report del task.

- [ ] **Step 3: Ruff sullo script**

Run: `cd D:/Claude/orchestrator && python -m ruff check scripts` — expected: `All checks passed!`

- [ ] **Step 4: Full suite (nessuna regressione)**

Run: `cd D:/Claude/orchestrator && python -m pytest -v` — expected: 73/73 passing (invariato, questo
task non tocca test).

- [ ] **Step 5: Commit**

```bash
git add scripts/derive_vol_thresholds.py
git commit -m "feat: script di derivazione soglie vol da dati OKX/Kraken reali (M1.5)"
```

---

## REVIEW BLOCCANTE — fermarsi qui

Prima di procedere al Task 3: mostrare ad Andrea l'output completo dello script (entrambi gli
asset, fonte dati usata, eventuale sanity check OKX/Kraken, soglie proposte e tabella di metriche
vs criterio pre-registrato). Attendere conferma esplicita. Non toccare `config/regime.yaml` prima
di questo via libera.

---

### Task 3: Config + provenienza + commit/push finale

**Files:**
- Modify: `config/regime.yaml`
- Create: `docs/regime-threshold-provenance-2026-07.md`

**Interfaces:**
- Consumes: i valori `enter`/`exit` approvati da Andrea nella review bloccante del Task 2 (NON i
  valori di esempio di questo piano — quelli reali derivati dallo script contro dati di mercato
  veri, sourced dal report del Task 2 dopo l'ok esplicito).

- [ ] **Step 1: Aggiornare `config/regime.yaml`**

Sostituire `ewma_span: 20` con `ewma_span: 32`, e sostituire `enter_threshold`/`exit_threshold` di
`btc` ed `eth` con i valori esatti approvati da Andrea nella review bloccante (leggerli dal report
del Task 2, sezione "Soglia proposta" per ciascun asset — NON inventare né arrotondare senza dirlo).
Aggiornare anche i commenti inline per riflettere la nuova provenienza:

```yaml
# Soglie regime layer v0 (ADR-036 §3) — regole semplici, isteresi obbligatoria.
# ewma_span=32 ancorato a RiskMetrics lambda=0.94 (vedi docs/nota-ancoraggio-soglie-vol-regime-2026-07.md).
# Soglie enter/exit derivate offline da storia reale BTC/ETH (M1.5, vedi
# docs/regime-threshold-provenance-2026-07.md per data derivazione, dati usati, prossima ri-derivazione).
vol:
  ewma_span: 32
  btc:
    enter_threshold: <VALORE APPROVATO DAL TASK 2 PER BTC>
    exit_threshold: <VALORE APPROVATO DAL TASK 2 PER BTC>
  eth:
    enter_threshold: <VALORE APPROVATO DAL TASK 2 PER ETH>
    exit_threshold: <VALORE APPROVATO DAL TASK 2 PER ETH>
funding:
  eth:
    enter_threshold: 0.0005   # invariato, fuori scope M1.5
    exit_threshold: 0.0002
```

- [ ] **Step 2: Verificare che il config loader legga ancora correttamente i nuovi valori**

Run: `cd D:/Claude/orchestrator && python -m pytest tests/regime/test_config.py -v`

`tests/regime/test_config.py` asserisce valori specifici (`0.80`, `0.60`, `1.00`, `0.75` per gli
enter/exit threshold correnti) che ora saranno diversi. Aggiornare quelle asserzioni ai nuovi
valori esatti approvati — questo è l'unico file di test da toccare in questo task, e va aggiornato
per riflettere i nuovi numeri reali, non per aggirare il test.

- [ ] **Step 3: Creare il documento di provenienza**

```markdown
# Provenienza soglie vol regime — derivazione 2026-07 (M1.5)

**Data derivazione:** [data di esecuzione reale del Task 2]
**Prossima ri-derivazione:** [data derivazione + 3 mesi]
**Script:** `scripts/derive_vol_thresholds.py`
**Piano:** `docs/superpowers/plans/2026-07-05-m1.5-vol-threshold-derivation.md`

## Dati usati

- BTC: [fonte esatta riportata dallo script — OKX puro o OKX+Kraken con date], periodo
  [data inizio] - [data fine], [N] osservazioni di rendimento.
- ETH: [idem].
- Se fallback Kraken attivato: correlazione rendimenti OKX vs Kraken nel periodo di overlap =
  [valore riportato dallo script].

## Stimatore

EWMA su rendimenti giornalieri, `span=32` (ancoraggio RiskMetrics λ=0.94, half-life ≈11.2 giorni —
vedi `docs/nota-ancoraggio-soglie-vol-regime-2026-07.md`), annualizzato ×√365.

## Criterio (pre-registrato, ADR-036 §3 / piano M1.5)

Frazione target tempo in high-vol: 20-25%. Vincoli aggiuntivi sui candidati: mediana transizioni/anno
<= 8, dwell time minimo osservato >= 3 giorni.

## Soglie scelte

| Asset | enter | exit | frazione effettiva | mediana transizioni/anno | dwell time minimo |
|---|---|---|---|---|---|
| BTC | [valore] | [valore] | [valore]% | [valore] | [valore] gg |
| ETH | [valore] | [valore] | [valore]% | [valore] | [valore] gg |

## Limiti dichiarati

- Nessun segnale combinato BTC+ETH: le due soglie sono derivate e valutate indipendentemente
  (chiarito esplicitamente con Andrea durante la scrittura del piano — un'eventuale combinazione
  è una decisione di design di M2).
- Nessun backtest/PnL: le metriche riportate misurano solo il comportamento della macchina a stati
  sulla storia (frazione di tempo, transizioni, dwell time), mai un risultato economico.
- Ri-derivazione programmata a 3 mesi da questa data: se la distribuzione di vol di BTC/ETH si sposta
  in modo strutturale nel frattempo, le soglie assolute qui fissate potrebbero richiedere
  aggiornamento anticipato — vedi anche `docs/nota-ancoraggio-soglie-vol-regime-2026-07.md` §2 sulla
  preferenza della letteratura per soglie percentili rolling rispetto a valori assoluti fissi.
```

Compilare tutti i campi tra parentesi quadre con i valori reali dal report approvato del Task 2 —
nessun campo va lasciato con il placeholder letterale.

- [ ] **Step 4: Full suite + ruff**

Run: `cd D:/Claude/orchestrator && python -m pytest -v` — expected: 73/73 passing.
Run: `cd D:/Claude/orchestrator && python -m ruff check src tests scripts` — expected: `All checks passed!`

- [ ] **Step 5: Commit**

```bash
git add config/regime.yaml docs/regime-threshold-provenance-2026-07.md tests/regime/test_config.py
git commit -m "feat: soglie vol regime derivate da storia reale BTC/ETH (M1.5)"
```

- [ ] **Step 6: Push e verifica**

```bash
git push origin master
git ls-remote origin refs/heads/master
git log -1 --format="%H %s"
```

Confermare che l'hash restituito da `git ls-remote` coincide con l'ultimo commit locale.

---

## Self-Review

**Spec coverage:** i 4 punti della richiesta sono coperti: (1) script di derivazione con test sui
componenti puri → Task 1+2; (2) review bloccante prima di toccare il config → sezione dedicata tra
Task 2 e Task 3; (3) aggiornamento config + documento di provenienza + suite verde → Task 3 Step
1-4; (4) commit + push, master==origin/master → Task 3 Step 5-6.

**Placeholder scan:** gli unici placeholder rimasti sono nel template YAML/markdown del Task 3
(valori enter/exit/date), esplicitamente perché sono l'OUTPUT del Task 2 — non ancora noti alla
scrittura del piano per costruzione (la derivazione da dati reali è il punto della milestone).
Marcati chiaramente con istruzioni su dove trovare il valore reale, non lasciati vaghi.

**Type consistency:** `ThresholdCandidateMetrics`, `ThresholdSearchCriterion`,
`search_threshold_candidates`, `evaluate_threshold_candidate` hanno la stessa firma in Task 1
(dove sono definiti) e Task 2 (dove sono consumati) — verificato manualmente.
